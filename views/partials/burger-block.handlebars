<button type="button" class="change-devoured {{#if devoured}}btn btn-secondary btn-lg {{else}}btn btn-primary btn-lg{{/if}}" data-id="{{id}}" data-devoured="{{devoured}}">
    {{burger_name}}
</button>