<button type="button" class="change-devoured m-1 {{#if devoured}}btn btn-secondary btn-lg {{else}}btn btn-success btn-lg{{/if}}" data-id="{{id}}" data-devoured="{{devoured}}">
    {{burger_name}}
</button>